## Project-specific cargo config
# NOTE: Avoid forcing a global target here because tools like `bootloader build`
# need to compile host-side crates for the host triple. We'll pass
# `--target x86_64-unknown-none` explicitly when building the kernel.

