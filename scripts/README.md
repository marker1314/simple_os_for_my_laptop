# 스크립트 디렉토리

이 디렉토리에는 빌드 및 실행을 위한 스크립트가 포함됩니다.

## 예정된 스크립트

- `run.sh` / `run.bat`: QEMU에서 커널 실행
- `build.sh` / `build.bat`: 커널 빌드
- `test.sh` / `test.bat`: 테스트 실행
- `debug.sh` / `debug.bat`: 디버깅 모드 실행

